{{- if .Values.enabled | default false }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana.fullname" . }}-test-connection"
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['grafana:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}
