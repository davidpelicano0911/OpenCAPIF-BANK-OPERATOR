apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocf-register.fullname" . }}-test-connection"
  labels:
    {{- include "ocf-register.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['register:{{ .Values.service.port }}']
  restartPolicy: Never
