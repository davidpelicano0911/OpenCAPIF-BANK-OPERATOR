apiVersion: v1
kind: Service
metadata:
  name: api-invocation-logs
  labels:
    {{- include "ocf-api-invocation-logs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocf-api-invocation-logs.selectorLabels" . | nindent 4 }}
