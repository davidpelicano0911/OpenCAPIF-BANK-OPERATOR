apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocf-security.fullname" . }}-test-connection"
  labels:
    {{- include "ocf-security.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['capif-security:{{ .Values.service.port }}']
  restartPolicy: Never
