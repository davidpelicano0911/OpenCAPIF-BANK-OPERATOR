apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocf-routing-info.fullname" . }}-test-connection"
  labels:
    {{- include "ocf-routing-info.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['capif-routing-info:{{ .Values.service.port }}']
  restartPolicy: Never
