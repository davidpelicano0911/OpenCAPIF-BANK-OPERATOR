apiVersion: v1
kind: Service
metadata:
  name: api-invoker-management
  labels:
    {{- include "ocf-api-invoker-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocf-api-invoker-management.selectorLabels" . | nindent 4 }}
