apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocf-helper.fullname" . }}-test-connection"
  labels:
    {{- include "ocf-helper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['helper:{{ .Values.service.port }}']
  restartPolicy: Never
