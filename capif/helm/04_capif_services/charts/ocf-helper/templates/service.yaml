apiVersion: v1
kind: Service
metadata:
  name: helper
  labels:
    {{- include "ocf-helper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocf-helper.selectorLabels" . | nindent 4 }}
