apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocf-open-discover-service-api.fullname" . }}-test-connection"
  labels:
    {{- include "ocf-open-discover-service-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['open-discover-service-apis:{{ .Values.service.port }}']
  restartPolicy: Never
