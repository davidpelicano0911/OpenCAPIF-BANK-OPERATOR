apiVersion: v1
kind: Service
metadata:
  name: published-apis
  labels:
    {{- include "ocf-publish-service-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocf-publish-service-api.selectorLabels" . | nindent 4 }}
