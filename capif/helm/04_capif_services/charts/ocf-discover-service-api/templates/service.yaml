apiVersion: v1
kind: Service
metadata:
  name: service-apis
  labels:
    {{- include "ocf-discover-service-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocf-discover-service-api.selectorLabels" . | nindent 4 }}
